package token

/*
func IsTokenAvailable(token string) bool {
	for _, t := range availableTokens {
		if token == t {
			return true
		}
	}
	return false
}

func getTypeAsString(v Variable.Variable) string {
	if v.IsNumeric {
		return "Int"
	} else if v.IsString {
		return "String"
	}
	return "<nil>"
}

func getVarMutationStateAsString(v Variable.Variable) string {
	if v.IsMutable {
		return "Mutable"
	}
	return "Const"
}

func ShowVariables() {
	Logger.Info(-1, "VARIABLES:\n\n")
	for key := range availableVariables {
		Logger.Debug("-------------------------")
		Logger.Debug(fmt.Sprintf("NAME: %s", key))
		Logger.Debug(fmt.Sprintf("TYPE: %s", getTypeAsString(availableVariables[key])))
		Logger.Debug(fmt.Sprintf("MUTATION: %s", getVarMutationStateAsString(availableVariables[key])))
		Logger.Debug(fmt.Sprintf("VALUE: %v", availableVariables[key].Value))
		Logger.Debug("-------------------------\n\n")
	}
}
*/
